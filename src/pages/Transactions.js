const Transactions = () => {
    return (
        <div>
            <h1 className="text-xl font-bold">Transactions</h1>
            <p>View and manage all transactions here.</p>
        </div>
    );
};

export default Transactions;
